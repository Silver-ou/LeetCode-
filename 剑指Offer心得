1.  获取字符串长度:
      str.length();

2.  如何遍历一个字符串:
      for(int i=0;i<s.length();i++){
              s.charAt(i);  -->即为字符串s第i个位置的字符
      }
      
3.  如何截断字符串:
      s.substring(a,b);  --> 截断字符串s从a到b的字串（包括a的位置但不包括b的位置）

4.  如何在字符串指定位置插入字符或字符串:
      s.insert(i,str);   -->在第i个位置插入str
      
5.  在末尾处添加字符串:
      s.append(str);
      
6.   StringBuffer转成String：
      s = stringbuffer.toString();

7.  关于Arraylist用法:
      创建一个新的arraylist: ArrayList<Integer> list = new ArrayList<>();
      在指定位置插入值: arraylist.add(index,val);  --> 在第index个位置插入val
      arraylist.add(val);  -->直接在后面插入值
 
8.   Java中，Stack 是一个类，而Queue是一个接口。
      创建一个stack : Stack<Integer> stack = new Stack<>();
      创建一个Queue: 
                  import java.util.Queue；
                  import java.util.LinkedList;
                  Queue<Integer> queue = new LinkedList<>();
                        queue.offer(val) -->相当于push操作
                        queue.poll() -->相当于pop操作
                        queue.isEmpty()  -->判断是否为空
 
 
